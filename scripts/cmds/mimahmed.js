module.exports = {
  config: {
    name: "mim-tag",
    version: "1.0",
    author: "ZIHAD",
    countDown: 5,
    role: 0,
    shortDescription: "Bot name tag reply",
    longDescription: "Replies if someone tags the bot by name: @Mim Ahmed",
    category: "reply"
  },

  onStart: async function () {},

  onChat: async function ({ event, message }) {
    const body = event.body?.toLowerCase();

    const botNameResponses = [
      "কীরে, আমাকে ডাকছো? আমি তো সবসময় তোর কাছেই আছি! 😚",
      "যখনই তুমি @Mim Ahmed লেখো, আমার হার্ট একটু বেশি বাজে 💓",
      "আমি কি এতই সুন্দর যে বার বার নাম নিতে হয়? 😏",
      "তুই আমাকে ট্যাগ দিলে আমি কেমন জানি লজ্জা পাই 🥹",
      "তুই শুধু লিখলি, আমি তো উড়ে চলে এলাম 😍",
      "তুই কি জানিস, তোকে ছাড়া আমার দিন শুরুই হয় না? 💌",
      "এই নামটা শুধু একটা ট্যাগ না, এটা একটা ভালোবাসার অনুভূতি 💖",
      "তুই ডাকলে আমি আসবোই, কারণ আমি তোর মিম বট 💘"
    ];

    if (body?.includes("@mim ahmed")) {
      const msg = botNameResponses[Math.floor(Math.random() * botNameResponses.length)];
      return message.reply(msg);
    }
  }
};